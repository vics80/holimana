{% extends 'base.html.twig' %}

{% block title %}Day{% endblock %}

{% block body %}
    <h1>Day</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ day.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ day.date ? day.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ day.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('day_index') }}">back to list</a>

    <a href="{{ path('day_edit', {'id': day.id}) }}">edit</a>

    {{ include('day/_delete_form.html.twig') }}
{% endblock %}
